import { AutoModelForCTC, Wav2Vec2FeatureExtractor } from 'https://cdn.jsdelivr.net/npm/@huggingface/transformers@4.2.0';
const ID='qnighy/wav2vec2-xlsr-53-espeak-cv-ft-ONNX';let model=null,extractor=null,vocab=null,loading=null;
function progress(x){if(x?.status==='progress'&&x.progress!=null)postMessage({type:'status',state:'loading',title:`Downloading deep sound model… ${Math.round(x.progress)}%`,detail:x.file||'IPA phoneme recognizer · q4'});}
async function load(){if(model&&extractor&&vocab)return;if(loading)return loading;postMessage({type:'status',state:'loading',title:'Loading deep sound checker…',detail:'Only used for -ed, -s/-es and j/ch targets. First use is a larger download (~230 MB), then it is cached.'});loading=(async()=>{model=await AutoModelForCTC.from_pretrained(ID,{dtype:'q4',progress_callback:progress});extractor=await Wav2Vec2FeatureExtractor.from_pretrained(ID,{progress_callback:progress});const obj=await (await fetch(`https://huggingface.co/${ID}/resolve/main/vocab.json`)).json();vocab=[];for(const [token,i] of Object.entries(obj))vocab[i]=token;postMessage({type:'status',state:'ready'})})();try{await loading}catch(e){model=extractor=vocab=null;loading=null;throw e}}
async function transcribe(pcm){await load();const input=await extractor(pcm);const {logits}=await model(input);const [,frames,size]=logits.dims,d=logits.data,out=[];let prev=-1;for(let t=0;t<frames;t++){let best=0;for(let v=1;v<size;v++)if(d[t*size+v]>d[t*size+best])best=v;if(best===prev)continue;prev=best;if(vocab[best]&&vocab[best]!=='<pad>')out.push(vocab[best])}return out.join(' ')}
self.onmessage=async e=>{const m=e.data||{};if(m.type!=='phonemes')return;try{const ipa=await transcribe(new Float32Array(m.audio));postMessage({type:'result',id:m.id,ipa})}catch(err){postMessage({type:'error',id:m.id,error:err?.message||String(err)})}};
